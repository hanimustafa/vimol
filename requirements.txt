# vimol runtime dependencies.
#
# vimol is pure Python; numpy is its only required third-party package.
# Everything else it uses (argparse, base64, dataclasses, os, select, sys,
# time, typing, zlib) is in the standard library.
#
# Install the package itself with `pip install vimol` (or `pip install -e .`
# from a checkout); this file pins the same requirement for reproducible
# environments and CI.
numpy>=1.20

# Optional extras (not installed by this file):
#   OpenGL backend      pip install "vimol[gl]"    ->  moderngl>=5.6
#   test suite          pip install "vimol[test]"  ->  pytest>=7
